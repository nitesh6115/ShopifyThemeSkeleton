{% doc %}
  Divider Block
{% enddoc %}

<div
  class="
    divider-block
    align--{{ block.settings.align }}
    {% if block.settings.hide_mobile %}hide-mobile{% endif %}
    {% if block.settings.hide_desktop %}hide-desktop{% endif %}
  "

  style="
    --bw: {{ block.settings.border }}px;
    --bc: {{ block.settings.color }};
    --w: {{ block.settings.width }}%;

    --pt: {{ block.settings.pt }}px;
    --pb: {{ block.settings.pb }}px;

    --ptm: {{ block.settings.ptm }}px;
    --pbm: {{ block.settings.pbm }}px;
    {% case block.settings.width %}
      {% when 'fit-content' %}
      --item-width:auto;
      {% when 'fill' %}
      --item-width:fill;
      {% when 'custom' %}
      --item-width:{{ block.settings.custom_width }}%;
      {% endcase %}

      {% case block.settings.width_mobile %}
      {% when 'fit-content' %}
      --item-width-m:auto;
      {% when 'fill' %}
      --item-width-m:fill;
      {% when 'custom' %}
      --item-width-m:{{ block.settings.custom_width_mobile }}%;
      {% endcase %}

      {% case block.settings.height %}
      {% when 'fit' %}
      --item-height:auto;
      {% when 'fill' %}
      --item-height:fill;
      {% when 'custom' %}
      --item-height:{{ block.settings.custom_height }}%;
      {% endcase %}
  "

  {{ block.shopify_attributes }}
>
  <div class="divider-line">&nbsp;</div>
</div>

{% stylesheet %}
  .divider-block {
    width: 100%;
    padding-top: var(--pt);
    padding-bottom: var(--pb);
  }
  /* line */
  .divider-line {
    width: var(--w);
    border-top: var(--bw) solid var(--bc);
  }
  /* align */
  .align--left .divider-line {
    margin-right: auto;
  }
  .align--center .divider-line {
    margin-left: auto;
    margin-right: auto;
  }
  .align--right .divider-line {
    margin-left: auto;
  }
  /* mobile */
  @media (max-width:767px){
    .divider-block {
      padding-top: var(--ptm);
      padding-bottom: var(--pbm);
    }
  }
  /* hide */
  @media (min-width:768px){
    .hide-desktop{ display:none !important; }
  }
  @media (max-width:767px){
    .hide-mobile{ display:none !important; }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Divider",
  "tag": null,
  "settings": [
    {
      "type": "range",
      "id": "border",
      "label": "Border size",
      "min": 1,
      "max": 10,
      "step": 1,
      "default": 1
    },
    {
      "type": "color",
      "id": "color",
      "label": "Border color",
      "default": "#000000"
    },
    {
      "type": "select",
      "id": "width",
      "label": "Width Desktop",
      "options": [
        {
          "value": "fit-content",
          "label": "Fit"
        },
        {
          "value": "fill",
          "label": "Fill"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "fill"
    },
    {
      "type": "range",
      "id": "custom_width",
      "label": "Custom Width Desktop",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 100,
      "visible_if": "{{ block.settings.width == 'custom' }}"
    },
    {
      "type": "select",
      "id": "width_tablet",
      "label": "Width Tablet",
      "options": [
        {
          "value": "fit-content",
          "label": "Fit"
        },
        {
          "value": "fill",
          "label": "Fill"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "fill"
    },
    {
      "type": "range",
      "id": "custom_width_tablet",
      "label": "Custom Width Tablet",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 100,
      "visible_if": "{{ block.settings.width_tablet == 'custom' }}"
    },
    {
      "type": "select",
      "id": "width_mobile",
      "label": "Width Mobile",
      "options": [
        {
          "value": "fit-content",
          "label": "Fit"
        },
        {
          "value": "fill",
          "label": "Fill"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "fill"
    },
    {
      "type": "range",
      "id": "custom_width_mobile",
      "label": "Custom Width Mobile",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 100,
      "visible_if": "{{ block.settings.width_mobile == 'custom' }}"
    },
    {
      "type": "select",
      "id": "height",
      "label": "Height",
      "options": [
        {
          "value": "fit",
          "label": "Fit"
        },
        {
          "value": "fill",
          "label": "Fill"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ],
      "default": "fit"
    },
    {
      "type": "range",
      "id": "custom_height",
      "label": "Custom Height",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 100,
      "visible_if": "{{ block.settings.height == 'custom' }}"
    },
    {
      "type": "select",
      "id": "align",
      "label": "Alignment",
      "default": "center",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ]
    },

    {
      "type": "header",
      "content": "Padding Desktop"
    },

    {
      "type": "range",
      "id": "pt",
      "label": "Top",
      "min": 0,
      "max": 200,
      "step": 2,
      "default": 10
    },

    {
      "type": "range",
      "id": "pb",
      "label": "Bottom",
      "min": 0,
      "max": 200,
      "step": 2,
      "default": 10
    },

    {
      "type": "header",
      "content": "Padding Mobile"
    },

    {
      "type": "range",
      "id": "ptm",
      "label": "Top",
      "min": 0,
      "max": 150,
      "step": 2,
      "default": 8
    },

    {
      "type": "range",
      "id": "pbm",
      "label": "Bottom",
      "min": 0,
      "max": 150,
      "step": 2,
      "default": 8
    },

    {
      "type": "header",
      "content": "Visibility"
    },

    {
      "type": "checkbox",
      "id": "hide_desktop",
      "label": "Hide desktop"
    },

    {
      "type": "checkbox",
      "id": "hide_mobile",
      "label": "Hide mobile"
    }
  ],
  "presets": [{ "name": "Divider" }]
}
{% endschema %}
